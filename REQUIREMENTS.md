# 需求说明：ESP32-C3 温湿度气压监控 Dash

| 项 | 内容 |
|----|------|
| 项目目录 | `G:\esp32s3\esp32c3_sensors-THP-dash` |
| 文档性质 | 需求与方案定稿（只约定「做什么 / 约束是什么」，不含实现代码） |
| 状态 | 已按讨论逐项收束；实现阶段以此为准 |

---

## 1. 项目目标

构建一套**远程温湿度、气压监控系统**：

- 现场：ESP32-C3 采集温湿度与气压，按固定间隔经 **HTTPS** 上报。
- 云端：**Cloudflare Workers** 提供 API，**Cloudflare D1** 持久化数据。
- 展示：**Cloudflare Pages** 上的 Web Dashboard（下称 **Dash**），登录后查看曲线、当前值，可导出 CSV，可管理设备上报 Token。

一句话：**多设备预留的 THP 时序采集 + 全接口鉴权的私有监控 Dash。**

---

## 2. 范围

### 2.1 范围内

- ESP32-C3 + SHT40 + BMP280 数据采集与 HTTPS 上报
- 设备级 API Token 上报鉴权
- 用户名/密码自建登录（**不使用** Cloudflare Access）
- D1 中原始读数**永久保留**
- Dash：默认当天，可选时间范围；综合三线图与分项三图可切换
- 长时间范围**自动降采样**（展示与 CSV 同一粒度策略）
- 按时间范围导出 CSV
- 登录后的页面内**生成 / 吊销**设备上报 Token
- **全部业务 API 鉴权**（设备 token 或用户会话，无匿名读口）

### 2.2 范围外（明确不做）

- Cloudflare Access / Zero Trust 作为 Dash 登录方案
- 设备端复杂业务逻辑、OTA 产品化（预留即可，非本期必做）
- 实时 WebSocket 推送（Dash 采用轮询即可）
- 多租户 SaaS、公开匿名数据分享页
- 将 D1 替换为 InfluxDB 等专用时序库

---

## 3. 总体架构

```
[SHT40 温湿度] ──I2C──┐
                      ├── [ESP32-C3] ──HTTPS POST + Bearer 设备 Token──► [CF Worker API]
[BMP280 气压]  ──I2C──┘                                                      │
                                                                              ▼
                                                                           [D1]
                                                                              ▲
[浏览器] ──登录 Cookie──► [Pages Dash] ──全部请求经用户会话──► [CF Worker API] ─┘
```

### 3.1 职责划分

| 组件 | 职责 |
|------|------|
| ESP32-C3 | 采样、打包 JSON、HTTPS 上报；不直连 D1 |
| CF Workers | 鉴权、校验、读写 D1、自动降采样、CSV 导出、Token/用户管理 API |
| CF D1 | 唯一业务数据存储（读数、设备、token 哈希、用户、会话） |
| Pages Dash | 登录 UI、时间范围、双视图曲线、当前值、Token 管理、CSV 下载触发 |

### 3.2 部署形态约束

- Dash（Pages）与 API（Workers）应规划为**同主域**，便于 Cookie 会话与减少 CORS 复杂度。
- 设备只访问上报 API；人只通过 Dash 访问查询/管理 API。

---

## 4. 硬件与采集需求

### 4.1 硬件

| 器件 | 作用 | 接口 |
|------|------|------|
| ESP32-C3 | MCU + Wi-Fi | — |
| SHT40 | **温度、湿度**权威来源 | I2C（典型地址 `0x44`） |
| BMP280 | **气压**权威来源 | I2C（典型地址 `0x76` 或 `0x77`） |

### 4.2 量测口径（必须遵守）

| 字段 | 来源 | 单位 |
|------|------|------|
| 温度 | **仅** SHT40 | °C |
| 湿度 | **仅** SHT40 | %RH |
| 气压 | **仅** BMP280 | hPa |

- BMP280 内部温度**不得**作为对外「当前温度」展示或写入业务字段的主口径。
- 两传感器可共用同一 I2C 总线；地址冲突属接线/器件问题，不改变软件口径。

### 4.3 采集与上报节奏

| 项 | 要求 |
|----|------|
| 上报周期 | **5 分钟 / 次** |
| 日数据量 | **288 条 / 设备 / 天** |
| 传输 | **HTTPS**（TLS） |
| 方法与路径（概念） | `POST /api/v1/readings` |
| 鉴权 | 请求头 `Authorization: Bearer <device_api_token>` |
| 时钟 | 设备侧尽量 NTP；**入库与查询以服务端时间为准**（另存设备 `measured_at` 作对照） |

### 4.4 上报载荷（逻辑字段）

设备请求体需能表达：

| 字段 | 必填 | 说明 |
|------|------|------|
| `temperature` | 是 | SHT40 温度，°C |
| `humidity` | 是 | SHT40 湿度，%RH |
| `pressure` | 是 | BMP280 气压，hPa |
| `measured_at` | 建议 | 设备侧采样时间（ISO-8601，建议 UTC） |
| `device_id` | 可选 | 若发送，必须与 Token 绑定设备一致；**以 Token 绑定设备为准** |
| `rssi` / `battery` 等 | 否 | 可选诊断字段，非本期展示必填 |

服务端落库时额外写入：`received_at`（服务端时间）、解析后的 `device_id`。

---

## 5. 数据需求

### 5.1 保留策略（已拍板）

- **原始读数永久保留**（不自动删除 `readings` 明细）。
- 长历史展示与导出依靠**自动降采样**，而不是删除原始数据。
- 账号侧仍建议定期导出 D1 作冷备（工程要求，非功能按钮必做）。

### 5.2 逻辑数据模型

> 下列为需求级模型，字段名供实现对齐；实现可用等价命名。

**`readings`（原始读数，永久）**

| 字段 | 说明 |
|------|------|
| `id` | 主键 |
| `device_id` | 设备 |
| `ts` | **服务端**接收/归属时间，查询与 CSV 主时间轴 |
| `temperature` | °C |
| `humidity` | %RH |
| `pressure` | hPa |
| `measured_at` | 可选，设备时间 |
| `rssi` 等 | 可选 |

**索引需求**：至少 `(device_id, ts)`，服务所有按设备 + 时间范围的查询与导出。

**`devices`**

| 字段 | 说明 |
|------|------|
| `id` | 设备 ID |
| `name` | 展示名 |
| `status` | 如 active / disabled |
| `created_at` | 创建时间 |
| `last_seen_at` | 最近一次成功上报时间 |

**`api_tokens`（设备上报 Token）**

| 字段 | 说明 |
|------|------|
| `id` | Token ID |
| `device_id` | 绑定设备 |
| `token_hash` | **仅存哈希**，明文只在生成时展示一次 |
| `created_at` | 创建时间 |
| `revoked_at` | 吊销时间，非空即失效 |
| `last_used_at` | 最近使用时间 |

**`users`（自建登录）**

| 字段 | 说明 |
|------|------|
| `id` | 用户 ID |
| `username` | 唯一 |
| `password_hash` | 慢哈希（Argon2id 或 bcrypt 等） |
| `role` | 如 `admin` / `user`（Token 管理等需 admin） |
| `created_at` | 创建 |
| `disabled_at` | 停用 |

**`sessions`（用户会话）**

| 字段 | 说明 |
|------|------|
| `id` 或存储哈希 | 会话标识 |
| `user_id` | 用户 |
| `created_at` / `expires_at` | 生命周期 |
| `last_used_at` | 滑动续期用 |

### 5.3 多设备预留（已拍板）

- 读数、设备、Token 从第一天起均带 **`device_id` 维度**。
- 单设备运行时不影响架构；新增设备 = Dash 内创建设备 + 生成 Token + 固件配置。
- **禁止**在业务表中假设「全局只有一台设备」。

---

## 6. 鉴权需求

### 6.1 总原则（已拍板）

| 身份 | 使用场景 | 凭证 | 能力边界 |
|------|----------|------|----------|
| 设备 | 上报读数 | 设备 API Token（Bearer） | **仅** `POST` 读数 |
| 人 | Dash、查询、导出、Token/用户管理 | 自建登录 **会话 Cookie** | 全部业务 `GET` 与管理写操作 |

- **全部业务 API 必须鉴权**；不存在匿名查询、匿名导出、匿名 Token 列表。
- **不使用 Cloudflare Access**；用户体系自建（D1 `users` + `sessions` + Worker）。
- 设备 Token **仅用于上报**，不得用于打开 Dash 或管理接口。
- 用户会话 **不得**用于伪造设备上报（上报只认设备 Token）。

### 6.2 设备 Token（POST 上报）

| 项 | 要求 |
|----|------|
| 生成入口 | **Dash 登录后**的设备/Token 管理页 |
| 展示 | **明文仅生成时显示一次**；其后页面只显示 ID、掩码、设备、创建/吊销状态 |
| 存储 | 服务端只存 `token_hash` |
| 请求 | `Authorization: Bearer <token>` |
| 校验 | 有效、未吊销、绑定设备有效 |
| 吊销 | Dash 内操作；吊销后上报立即 `401/403` |
| body 伪造 | `device_id` 以 Token 绑定为准；不一致则拒绝 |

### 6.3 用户登录（自建，非 Access）

| 项 | 要求 |
|----|------|
| 入口 | Pages Dash 登录页 |
| 接口概念 | `POST /api/auth/login`、`POST /api/auth/logout`、`GET /api/auth/me` |
| 凭证 | 用户名 + 密码 |
| 校验 | 慢哈希比对；失败不得区分「用户不存在/密码错误」的过度细节 |
| 会话 Cookie | `HttpOnly`、`Secure`、`SameSite=Lax` 或 `Strict` |
| 有效期 | 实现默认建议约 **1 天滑动续期**（可配置） |
| 登出 | 服务端使会话失效 + 清 Cookie |
| 防爆破 | 登录失败限速/延迟 |
| CSRF | 写操作（登录、Token 管理、用户管理）需防护（SameSite + 双提交或等价机制） |
| 初始用户 | 部署时创建**第一个 admin**（引导或受控初始化流程） |

### 6.4 接口鉴权矩阵

| 接口（概念） | 方法 | 鉴权 |
|--------------|------|------|
| `/api/auth/login` | POST | 无会话（登录本身） |
| `/api/auth/logout` | POST | 用户会话 |
| `/api/auth/me` | GET | 用户会话 |
| `/api/v1/readings` | POST | **设备 Token** |
| `/api/v1/readings` | GET | 用户会话 |
| `/api/v1/latest` | GET | 用户会话 |
| `/api/v1/devices` | GET/POST/DELETE | 用户会话（管理动作建议 admin） |
| `/api/v1/tokens` | GET/POST/DELETE | 用户会话（建议 admin） |
| `/api/v1/export` | GET | 用户会话 |
| `/api/v1/stats` | GET | 用户会话（若提供） |

未通过鉴权：一律 `401`（未认证）或 `403`（已认证但无权限）。

---

## 7. 查询与自动降采样

### 7.1 查询参数（人侧）

| 参数 | 必填 | 说明 |
|------|------|------|
| `device_id` | 是 | 单设备查询 |
| `from` | 是 | 范围起点（ISO-8601，建议 UTC） |
| `to` | 是 | 范围终点 |

Dash 默认范围：**当天**（产品时区下的本地日界 → 当前时刻；存储与 API 统一 UTC，界面按选定时区渲染「当天」）。

### 7.2 自动降采样（已拍板）

- 用户**只选时间范围**，**不**先选粒度。
- 服务端按跨度自动选择粒度；**曲线数据与 CSV 导出使用同一自动粒度**。
- **D1 永久保存 5 分钟原始点**；降采样仅作用于读路径。

**推荐 v1 粒度阶梯（实现可微调，行为须保持「范围越长点越少」）：**

| 选择范围 | 自动粒度 | 每设备约点数 |
|----------|----------|--------------|
| ≤ 24 小时（含默认当天） | **原始 5 min** | ≤ ~288 |
| ≤ 7 天 | **15–30 min** | 约数百 |
| ≤ 30 天 | **1–2 小时** | 约数百 |
| ≤ 90 天 | **6 小时** | 约数百 |
| ≤ 1 年 | **1 天** | ~365 |
| > 1 年 | **1 天或 1 周** | 可控 |

### 7.3 聚合语义

| 字段 | v1 聚合 |
|------|---------|
| 时间戳 `ts` | 时间桶起点或中点（全库统一一种） |
| 温度 / 湿度 / 气压 | **平均值 avg** |
| 空桶 | 跳过或 null，**禁止**填 0 |
| min/max | v1 可不做；接口可预留扩展 |

- 综合图与分项图调用**同一查询结果**，避免两套图数据不一致。
- 聚合实现：v1 允许对原始表按桶计算；数据量上来后可增加物化聚合表，**对外 API 与 Dash 交互不变**。

### 7.4 查询响应（逻辑）

每条序列点：

- `ts`
- `temperature`
- `humidity`
- `pressure`

另可提供：`granularity`（实际粒度，便于 UI 提示）、设备 `last_seen` 等元数据。

---

## 8. Dashboard 需求

### 8.1 页面与权限

| 页面 | 权限 |
|------|------|
| 登录页 | 公开 |
| Dash 主界面 | 需登录会话 |
| 设备 / Token 管理 | 需登录；建议 admin |
| 数据 API | 见鉴权矩阵，无公开数据口 |

### 8.2 主界面功能

| 功能 | 要求 |
|------|------|
| 设备选择 | 列出全部 `devices`；可切换；单设备时仅一项 |
| 时间范围 | **默认当天**；预设：当天 / 近 24h / 近 7 天 / 近 30 天… + **自定义起止** |
| 视图模式 | **「综合」三线一张** ↔ **「分项」三张图**，用户可切换；切换不重置设备与时间范围 |
| 综合图 | 同一时间轴，温度、湿度、气压三线；**多 Y 轴**；图例可开关单条线 |
| 分项图 | 温度 / 湿度 / 气压 各一图，纵向排列；各自 Y 轴自适应 |
| 当前值 | 温度、湿度、气压大数字展示 |
| 在线状态 | 根据 `last_seen` 判断；建议超过 **2×上报周期（10 分钟）** 未上报视为可能离线（阈值实现可调） |
| 降采样提示 | 非原始粒度时 UI 标明实际粒度（如「已按小时聚合」） |
| 导出入口 | 见第 9 节；按钮状态跟随当前 `device_id + from + to` |
| Token 管理 | 创建设备、生成上报 Token（明文一次）、列表、吊销 |

### 8.3 展示与交互约定

- 时间显示：存储 UTC，界面按明确时区渲染（实现选定，如 Asia/Shanghai）；避免「当天」边界歧义。
- 轮询：Dash 可用定时刷新最新数据；**不要求** WebSocket。
- 图表库不限定，需支持多序列与多轴；量纲标注清晰（°C、%RH、hPa）。

---

## 9. CSV 导出需求

### 9.1 范围与列（已拍板）

| 项 | 要求 |
|----|------|
| 触发 | 登录用户在 Dash 上导出 |
| 范围 | **与当前所选一致**：`device_id + from + to`（默认即当天） |
| 设备 | **一次一个 device_id**，多设备不混在一个 CSV |
| 鉴权 | **用户会话**（与查询 API 相同；不是设备 Token） |
| 实现护栏 | 服务端先 `COUNT`；行数将超上限或 SQL 降采样失败时**自动再降一档粒度**；原始行超过安全阈值时**拒绝全量 `.all()`**，改降档或返回 413/503 |

**固定列顺序：**

1. `timestamp`
2. `temperature`
3. `humidity`
4. `pressure`

### 9.2 与降采样关系（已拍板）

| 范围 | 导出内容 |
|------|----------|
| ≤ 24 小时（含当天） | **原始 5 分钟点** |
| 更长范围 | **自动降采样后的点**（列结构不变） |

- 界面需提示：长范围导出为聚合粒度；原始数据仍完整存于系统。
- 单次导出设**行数上限**（建议如 5 万行）；超限则报错并提示缩短范围或由服务端再降一档粒度。降采样后常规范围通常远低于上限。

### 9.3 文件与格式

| 项 | 要求 |
|----|------|
| 响应类型 | `text/csv`，`Content-Disposition` 附件 |
| 文件名建议 | `thp_{device}_{from}_{to}.csv` |
| 编码 | **UTF-8**（是否加 BOM 由实现定，需兼顾 Excel） |
| 行序 | `timestamp` 升序 |
| 缺测 | 空字段，禁止填 0 |
| 数值 | 约定固定小数位（如温度/湿度/气压 1–2 位），避免科学计数法噪音 |
| 时间 | 建议导出 **ISO-8601 UTC**（或带明确偏移）；文档/页内说明时区 |

---

## 10. 上报 API 需求（设备侧）

| 项 | 要求 |
|----|------|
| 路径概念 | `POST /api/v1/readings` |
| 鉴权 | Bearer 设备 Token，必填 |
| 成功 | `2xx`，写入 `readings`，更新设备 `last_seen` |
| 失败 | `401/403` 鉴权失败；`400` 载荷不合法；`4xx/5xx` 设备侧应重试策略（有限次） |
| 幂等 | 不要求严格幂等为 v1 必做；重复上报可能产生多行——固件避免无意义重发；后续可加去重键 |
| 写入字段 | 见 4.4；`device_id` 由 Token 解析 |
| 频率 | 预期 5 分钟/设备；服务端不按「过密」设计复杂限流，可对明显异常洪泛做基础保护 |

---

## 11. 容量与性能预期（约束实现）

| 项 | 量级 |
|----|------|
| 单设备写入 | 288 行/天（5 min） |
| 单设备一年原始 | ≈ 1.0×10⁵ 行 |
| 多设备 | 线性扩展；表设计已含 `device_id` |
| Dash 当天查询 | ≤ ~288 点/设备，直接原始点 |
| D1 免费额度匹配度 | 5 分钟节奏下写入压力极低，适配个人/小规模长期运行 |
| 永久存储 | 以索引 + 读路径降采样保证可用；必要时后加聚合表 |

---

## 12. 安全需求汇总

1. 密码仅存慢哈希；明文不进仓库、日志、前端。  
2. 设备 Token 明文仅生成时展示；库中仅哈希。  
3. 全业务 API 鉴权；无匿名数据面。  
4. 设备 Token 与用户会话**权限分离**。  
5. 会话 Cookie：`HttpOnly` + `Secure` + `SameSite`。  
6. 登录防爆破；写接口 CSRF 防护。  
7. 固件配置中的 Token 与 URL 不入库到公开仓库。  
8. 吊销立即生效。  
9. 「永久数据」依赖 Cloudflare 账号安全 + 定期 D1 导出备份（运维要求）。

---

## 13. 已拍板决策清单

| # | 决策 | 状态 |
|---|------|------|
| 1 | 硬件：ESP32-C3 + SHT40 + BMP280 | 已定 |
| 2 | 云端：CF Workers + D1 + Pages | 已定 |
| 3 | 上报：HTTPS，5 分钟/次，288 条/天 | 已定 |
| 4 | 上报字段：设备标识（Token 绑定）、时间戳、温度、湿度、气压 | 已定 |
| 5 | POST 上报使用 **设备 API Token** | 已定 |
| 6 | Token 在 **Dash 登录后页面生成/吊销**，明文仅一次 | 已定 |
| 7 | **多设备预留**（全链路 `device_id`） | 已定 |
| 8 | 原始数据 **永久保留** | 已定 |
| 9 | 长时间范围 **自动降采样**（图与 CSV 一致） | 已定 |
| 10 | 图表：**综合三线一张 + 分项三张** 可切换 | 已定 |
| 11 | Dash 默认范围：**当天**，可另选时间范围 | 已定 |
| 12 | Dash **必须登录**；**全部 API 鉴权** | 已定 |
| 13 | **不使用 Cloudflare Access**；**自建**用户/会话 | 已定 |
| 14 | CSV：时间戳, 温度, 湿度, 气压；按当前时间范围导出 | 已定 |
| 15 | 一次 CSV 导出 **单设备** | 已定 |

---

## 14. 实现阶段待定细节（不阻塞本需求定稿）

| # | 项 | 建议默认 |
|---|----|----------|
| 1 | 降采样阶梯具体数值 | 采用第 7.2 节 v1 表 |
| 2 | 聚合点是否附 min/max | v1 仅 avg |
| 3 | 用户会话有效期 | 约 1 天滑动续期 |
| 4 | 产品展示时区 | 如 Asia/Shanghai |
| 5 | CSV 是否带 UTF-8 BOM | 兼顾 Excel 后定 |
| 6 | 第一个 admin 的初始化方式 | 部署引导/受控初始化 |
| 7 | 离线阈值 | 默认约 2×上报周期 |
| 8 | 聚合是否物化 | v1 查询时计算，性能不足再物化 |

---

## 15. 建议实现顺序（供后续开发，非本阶段交付）

1. D1 表结构与 `(device_id, ts)` 索引  
2. 自建登录与会话（users/sessions）  
3. 设备与 Token 管理 API（登录后生成 Token，仅哈希入库）  
4. 设备上报 API（Bearer Token → 写 `readings`）  
5. 人侧查询 API + **自动降采样**  
6. Pages Dash：登录、设备选择、时间范围、综合/分项图、当前值  
7. CSV 导出（同范围、同鉴权、同粒度策略 + 行数上限）  
8. ESP32-C3 固件：SHT40+BMP280 采样 + HTTPS 上报  
9. 备份与运维：D1 定期导出说明  

---

## 16. 验收要点（实现完成后对照）

- [ ] 未登录不能打开数据 API；未持设备 Token 不能 POST 读数  
- [ ] 5 分钟上报落库，字段口径：T/H 来自 SHT40，P 来自 BMP280  
- [ ] 库中原始行永久保留；长范围查询自动降粗并在 UI 标明  
- [ ] Dash 默认当天；可切换综合/分项；数据同一套查询结果  
- [ ] 可生成设备 Token（仅一次明文）、可吊销、吊销后上报失败  
- [ ] 可新增第二台设备而无需改架构  
- [ ] CSV 列顺序正确；当天为原始点，长范围为降采样点；鉴权正确  
- [ ] 登录、会话、CSRF、Token 哈希等安全项符合第 12 节  

---

*本文档整理自项目讨论中已确认的产品与技术约束，作为后续设计与实现的唯一需求基线。*
